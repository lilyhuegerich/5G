# Turn Vulnerabilities "on or off"

## SSH Server
An ssh server with a username and password only login.
### On / allow vulnerability
```console
user@core:~$ ./ssh_on
```

### Off
```console / default
user@core:~$ ./ssh_off
```
